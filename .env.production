# Committed because it is PUBLIC BY DESIGN: the Turnstile site key ships in
# every visitor's HTML (only TURNSTILE_SECRET_KEY is a real secret — that one
# lives in Worker secrets, never here). Owner-provided widget for PR #25.
# A build/dashboard environment variable of the same name overrides this file.
NEXT_PUBLIC_TURNSTILE_SITE_KEY=0x4AAAAAAE9KeNj44QrpjEW1
