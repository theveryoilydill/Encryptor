name = "pgp-keybase-app"
compatibility_date = "2025-09-01"
compatibility_flags = ["nodejs_compat"]
main = "./workers/app.ts"

assets = {}

# To deploy:
#   1. npx wrangler login
#   2. npm run build
#   3. npm run deploy
#
# The Worker serves the React Router v8 SPA and proxies Keybase API calls
# server-side, avoiding browser CORS restrictions.
