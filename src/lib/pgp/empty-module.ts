// Stub module — keybase-proofs requires Node's 'dns' built-in for its DNS
// scraper, which doesn't exist in the browser. We never use that scraper
// (it's only for the DNS proof type), so an empty stub is safe.
export {};
